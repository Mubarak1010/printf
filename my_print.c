#include "main.h"

/**
 * print - prints numbers one by one
 * @i: the munber
 * Return: number
 */

int print(int i)
{
	if (i < 0)
	{
		_putchar('-');
	}

	if (i / 10)
	{
		_putchar(d / 10 + '0');
		_putchar(d % 10 + '0');
	}
}
